const WELCOME = "Dapp-Stack";
const MSG = "You will find all the necessary information to build a professional dapp on top of the ethereum EVM virtual machine";
const PINEBERRY = "By Muquifuler";

function Go(){
    location.replace("./postindex.html");
}

/*
let i=0, j=0;
function write(){
    var intervalo = setInterval(function() {
            document.getElementById("welcome").innerHTML += "<i id='w"+i+"'>"+WELCOME.substring(i, (i+1))+"</i>";
            //document.getElementById("w"+i).style = "color: rgb(122, "+((Math.random()*255)+1)+", "+((Math.random()*255)+1)+");";

            //document.getElementById("whitepaper-pineberry").innerHTML += PINEBERRY.substring(i, (i+1));
            i++;
            if(WELCOME.length <= i){
                clearInterval(intervalo);
            }
    }, 100);
/*
    var intervalo2 = setInterval(function() {
        document.getElementById("msg").innerHTML += MSG.substring(j, (j+1));
        j++;
        if(MSG.length <= j){
            clearInterval(intervalo2);
        }
    }, 5);*//*
}

function Go(){
    location.replace("./postindex.html");
}

write();*/